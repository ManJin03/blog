# 仅用于本地 wrangler pages dev 调试，已被 .gitignore 忽略
# 把本文件复制为 .dev.vars 使用
ADMIN_PASSWORD=admin123456
ADMIN_USERNAME=admin
SESSION_SECRET=local-dev-secret-change-me

# GitHub OAuth（普通账号登录）。本地调试需要一个回调地址为
# http://localhost:8880/api/login/github/callback 的 OAuth App（与线上分开建）。
# 未配置时点击"使用 GitHub 登录"会显示未配置提示页。
GITHUB_CLIENT_ID=your-local-github-client-id
GITHUB_CLIENT_SECRET=your-local-github-client-secret
# GITHUB_REDIRECT_URI=http://localhost:8880/api/login/github/callback
